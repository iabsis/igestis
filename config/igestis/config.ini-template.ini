; Use %u to remplace by the username.

MYSQL_HOST = "" ;uncomment if you don't want to use debian config
MYSQL_LOGIN = "" ;uncomment if you don't want to use debian config
MYSQL_PASSWORD = "" ;uncomment if you don't want to use debian config
MYSQL_DATABASE = "" ;uncomment if you don't want to use debian config

;USE_LDAP = true
;LDAP_URIS = "ldap://localhost:389"
;LDAP_BASE = "dc=example,dc=local"
;LDAP_ADMIN = "cn=admin,dc=example,dc=local"
;LDAP_PASSWORD = ""

;LDAP_EMPLOYEE_SCHEMA = "samba3, samba4, openchange, unix" 		//TODO : new value
;LDAP_EMPLOYEE_DN = "ou=Users,dc=example,dc=local" 				//TODO : new value
;LDAP_EMPLOYEE_RDN = "uid" 										//TODO : new value
;LDAP_CUSTOMER_SCHEMA = "samba3, samba4, openchange, unix" 		//TODO : new value
;LDAP_CUSTOMER_DN = "ou=Customers,dc=example,dc=local" 			//TODO : new value
;LDAP_CUSTOMER_RDN = "cn" 										//TODO : new value
;LDAP_SUPPLIER_SCHEMA = "samba3, samba4, openchange, unix" 		//TODO : new value
;LDAP_SUPPLIER_DN = "ou=Suppliers,dc=example,dc=local" 			//TODO : new value
;LDAP_SUPPLIER_RDN = "cn" 										//TODO : new value
;LDAP_GROUP_SCHEMA = "samba3, samba4, openchange, unix" 		//TODO : new value
;LDAP_GROUP_DN = "ou=Suppliers,dc=example,dc=local" 			//TODO : new value
;LDAP_GROUP_RDN = "cn" 										//TODO : new value

;LDAP_VERSION = 3
;LDAP_AUTO_IMPORT_USER = false
;LDAP_READONLY = true //TODO : new value
;LDAP_CUSTOM_BIND = false
;LDAP_CUSTOM_FIND = "(|(&(objectClass=user)(sAMAccountName=%u))(&(objectClass=posixAccount)(uid=%u)))"		//TODO : to be removed
;LDAP_USERS_OU = "ou=Users,dc=example,dc=local"  															//TODO : to be removed
;LDAP_CUSTOMERS_OU = "ou=Customers,dc=example,dc=local" 													//TODO : to be removed
;LDAP_SUPPLIERS_OU = "ou=Suppliers,dc=example,dc=local" 													//TODO : to be removed
;LDAP_AD_MODE = false 																						//TODO : to be removed
;LDAP_USER_RDN = false																						//TODO : to be removed

UNIX_LOGIN_SHELL = "/bin/bash"
UNIX_GID_NUMBER = "100"
UNIX_HOME_DIRECTORY = "/home/%u"
UNIX_MIN_UID = "1000"

SAMBA_PASSWORD_EXPIRATION = 60;						//Value in day
SAMBA_PROFILE_PATH = "\\serveur\profiles\%u"		//UNC Path 
SAMBA_HOME_DRIVE = "Z:"								//Default drive map (empty for not mapping)
SAMBA_HOME_PATH = "\\serveur\share\"				//Default path to map

IGESTIS_PASSWORD_POLICY = ""						//New Regex value for the password.

LOG_FILE = "log/"									//TODO : rename this var to LOG_FOLDER. Must be log under $ROOT$ but can be overrided.
CACHE_FOLDER = "cache/"								//TODO : must be data by default but can be overrided.
ENCRYPT_KEY = ""									//TODO : must be added in the configuration check

;IGESTIS_CORE_ADMIN = "root"						//TODO : must be placed in the database (super_user field)
;MIN_UID_NUMBER = 1000 								//TODO : to be renamed to UNIX_MIN_UID
;THEME = "iabsis_v2"								//TODO : to be removed.
;SERVER_FOLDER = "/usr/share" 						//TODO : to be removed.
;APPLI_FOLDER = "/igestis" 							//TODO : to be removed.
;WEB_SUBFOLDER = "/igestis"							//TODO : must be determined dynamically.
;DATA_FOLDER = "/usr/share/igestis/documents/" 		//TODO : must be documents by default but can be overrided.
;ROOT_FOLDER = "/usr/share/igestis"					//TODO : must be determined dynamically.
;AUTO_CSRF_PROTECTION = true						//TODO : to be removed.

